/** How often the monitored-call panel polls the Voice SDK for live call state. */
export const POLL_INTERVAL_MS = 2000;

/** Max characters shown for a task's last-message preview before truncating. */
export const TASK_CONTENT_MAX_LENGTH = 25;

/** Anonymous Conversations user SIDs start with this prefix and are this long. */
export const ANON_USER_SID_PREFIX = "FX";
export const ANON_USER_SID_LENGTH = 34;
